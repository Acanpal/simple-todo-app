# 今日の学び 2026-01-22
## 何をしたか
- Reactのコンポーネントの責務分離について学んだ
- dnd-kitについて軽く復習(自分のプロジェクトでどう動いているかくらい)

## 学んだこと
- 0120でも学んだが、リファクタリングとして責務分離を意識する
- 配列.filter(条件)で条件に合うものだけ抽出 返り値は配列
- 配列.map(条件)で条件に合うものだけ変換して返したり、値を変更して返す 返り値は配列
- dnd-kitのArrayMove → arrayMove(配列, 移動元インデックス, 移動先インデックス) 返り値は配列
- https://zenn.dev/wintyo/articles/d39841c63cc9c9

# 今日の学び 2026-01-23
## 何をしたか
- 自作のエラークラス(httpステータスや、判別用コードを含む)を作成して、フロント側で判別できるようにした
- そのエラークラスをもとに、エラーハンドリングを丁寧にした！
- Hook UI の責務分離について学んだ
- Expressの基礎を再確認(Node.jsでできることとExpressでできることの違い)

## まだ改善(勉強)できる点
- errorMessageをエラーの種類で別ファイルに分けるとわかりやすい？
- 実際に学んだエラーを引き起こしてみて、エラーメッセージが適切かを確認する
  - 204を返すようにする
  - json以外を返すようにする
- 各ファイルがどこに分類されるかをしっかり考える(apiに何入れればいいのか？)
- expressの学んだことを実装してみる
- expressでAPI設計をしっかり考える。

## 学んだこと
- 想定エラーは自作のエラー、想定外は通常エラーを返すとわかりやすい？
- 想定できなかったエラー
  - jsonで返ってくる想定だとしても、jsonが返ってこない場合もある
  - 204(No Content)が返ってくる場合もある(今回はAPI規約違反なのでエラーにする)
- super()は親クラスのコンストラクタを呼び出す
- ディレクトリ構成で api / components / pages / utils / hooks などに分けるとわかりやすい
- TypeScriptの型定義を少しだけ学んだ
 - interfaceで宣言できる(型に名前をつける)
- expressのerrorhandlingは特殊なmiddlewareという位置づけで実装する


# 今日の学び 2026-01-24
## 何をしたか
- フロントのrefactoring完了
  -責務分離についてかなりの把握ができた
  -hooks, App, components, pages のそれぞれの責務を抑えた。

## 学んだこと
- useTodosにすべてのAPI処理などを任せる
- 引数などの渡し方としてラッパー関数を各ページコンポーネントに用意する
- git restore --staged . (addの取り消し)
- useMemo, useEffectの使い方を学んだ

## まだ改善(勉強)できる点
- SortableTodoItemのコード内容をもう少し思い出す